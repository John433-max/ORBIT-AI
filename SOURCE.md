# Full source

**Complete archive (recommended):** download from the Grok project folder:

`artifacts/ORBIT-AI-source.zip` (~530 KB, no large `.npz` weights)

```bash
git clone https://github.com/John433-max/ORBIT-AI.git
cd ORBIT-AI
unzip /path/to/ORBIT-AI-source.zip
git add -A && git commit -m "Full ORBIT source" && git push
```

Or: GitHub → Add file → Upload files → drop unzipped contents.

This API-based upload continues in batches; the zip is the complete tree.

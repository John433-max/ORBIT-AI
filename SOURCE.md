# Full source

**Complete archive:** `artifacts/ORBIT-AI-source.zip` (~530 KB)

```bash
git clone https://github.com/John433-max/ORBIT-AI.git
cd ORBIT-AI
unzip /path/to/ORBIT-AI-source.zip
git add -A && git commit -m "Full ORBIT source" && git push
```

API upload continues in batches. Large modules (`agents.py`, `api.py`, `tools/chat_tool.py`, `tools_legacy.py`) are listed in `LARGE_FILES.md`.

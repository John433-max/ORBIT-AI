# Tree status

Flat repository root. Documentation is aligned to the actual codebase: **ORBIT is a modular local AI agent runtime**, not a 100B model.

Large modules on `main` should match full local sources (`agents.py`, `api.py`, `tools/chat_tool.py`, …).
